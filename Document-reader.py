# This line is calling the variable and calling the open function to open a specific file given in the string
# Insert your file name in the string, in this case, this is the second file which I uploaded in my repository
time_table_fd = open('test-read.txt')

# This prints the what the variable was told to do
print(time_table_fd)